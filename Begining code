#include <stdio.h>
#include <stdlib.h>
int main()
{
    int x, y, i;

    printf("Enter the number of processes : ");
    scanf("%d", &x);

    for(i = 0; i< x; i++)
        completed[i] = 0;

    printf("\n\nEnter the number of resources : ");
    scanf("%d", &y);

 
}
